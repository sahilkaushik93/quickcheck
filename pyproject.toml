[project]
name = "undq-transcript-mvp"
version = "0.2.0"
description = "MVP API for structured transcript metadata data-quality assessment"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
